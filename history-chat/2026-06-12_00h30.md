# Session 2026-06-12_00h30 — Allure Suites Fix + SonarCloud + CI Improvements

## Người thực hiện
- AI Agent (Antigravity) tự động trong khi user ngủ

## Vấn đề ban đầu
1. **Allure Suites hiện 7 items lộn xộn** — user muốn chỉ 3 suites: E2E, xUnit, Postman
2. **SonarCloud đã có workflow nhưng chưa hoàn chỉnh**
3. **build_site_index.py bị IndentationError** → CI fail step build landing page
4. **run_id extraction dùng grep fragile** → có thể miss trên JSON format khác

---

## Những gì đã làm

### 1. Fix Allure Suites → 3 nhóm
**File mới:** `Waste-Recycling-Platform/scripts/normalize_allure_suites.py`
- Script post-process tất cả `*-result.json` trong `allure-results/`
- Gán `parentSuite` label đúng cho từng file:
  - `E2E Tests` → TC-E2E-xxx, Frontend smoke, CodeceptJS/Playwright
  - `API Tests (Postman)` → Postman/Newman results
  - `Backend Tests (xUnit)` → xUnit, WastePlatform.Tests

**Thêm step trong `allure-gh-pages.yml`** (trước bước `allure generate`):
```yaml
- name: "Normalize Allure Suites → 3 groups (E2E / Postman / xUnit)"
  run: |
    python3 Waste-Recycling-Platform/scripts/normalize_allure_suites.py \
      Waste-Recycling-Platform/allure-results
```

### 2. Nâng cấp SonarCloud (`sonar.yml`)
- Thêm **code coverage** cho .NET (opencover XML)
- Thêm **unit test coverage** cho frontend (lcov.info)
- Thêm **SonarCloud cache** để tăng tốc CI
- Thêm **branch tracking** (`sonar.branch.name`)
- Tạo `Waste-Recycling-Platform/sonar-project.properties` cho frontend

### 3. Fix `build_site_index.py`
- Rewrite hoàn toàn với indentation đúng chuẩn
- Thêm E2E suite card (trước đó chỉ có 2 cards: API và xUnit)
- Thêm badges hiển thị: xUnit present, Postman present, History, Categories, Jira ratio

### 4. Fix `run_id` extraction → dùng `jq`
- Cả backend và E2E bước trong `allure-gh-pages.yml`
- Trước: `grep -o '"id": [0-9]+' | head -n1 | sed ...` (không tin cậy)
- Sau: `jq -r '.workflow_runs[0].id // empty'` (chính xác, nhất quán)

### 5. Tạo README.md
- Badges CI (Backend Tests, Frontend E2E, Postman Smoke, Allure, SonarCloud)
- Bảng team + KIEM task links
- Pipeline diagram ASCII
- Project structure

---

## Commits đã push
```
5b121bd16 feat: fix Allure suites to 3 groups + upgrade SonarCloud + fix build_site_index
f0a622219 feat: README + jq run_id fix + sonar-project.properties
```

---

## Checklist trạng thái CI
| Workflow | Status | Ghi chú |
|----------|--------|---------|
| Backend Tests | ✅ | xUnit 244+ tests |
| Frontend E2E | ⚠️ | Cần frontend server, continue-on-error |
| Postman Smoke | ✅ | Newman auto-login |
| Allure Pages | 🔄 | Fix jq + normalize step - chờ CI |
| CI CD Deploy | ⚠️ | E2E gate có continue-on-error |
| SonarCloud | 🔄 | Cần SONAR_TOKEN secret trong repo |

---

## Cần làm tiếp (nếu muốn)
1. **Thêm SONAR_TOKEN secret** vào GitHub repo settings → SonarCloud sẽ chạy được
2. **Tạo project trên SonarCloud** tại sonarcloud.io với organization `chi-trung`
3. **E2E với backend mock** — chạy được trong CI mà không cần server thật
