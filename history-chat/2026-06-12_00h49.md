# Session 2026-06-12_00h49 — E2E BDD Steps + SonarCloud Fix + Allure Suite Groups

## Thời gian
- Bắt đầu: ~00:30 (tiếp nối session trước)
- Kết thúc: ~00:54
- Người thực hiện: AI Agent (Antigravity) + user hỗ trợ

---

## Vấn đề được giải quyết

### 1. Allure Suites lộn xộn 7 items → fix thành 3 nhóm gọn
**Triệu chứng:** Allure report > Suites hiển thị 7 items:
- Frontend smoke, TC-E2E-002, TC-E2E-003, TC-E2E-004, Postman API Tests, xUnit Backend Tests, WastePlatform.Tests

**Giải pháp:**
- Tạo script `normalize_allure_suites.py` post-process `*-result.json`
- Gán `parentSuite` label đúng:
  - `E2E Tests` → tất cả E2E (TC-E2E-xxx, Frontend smoke)
  - `API Tests (Postman)` → Newman/Postman
  - `Backend Tests (xUnit)` → xUnit + WastePlatform.Tests
- Thêm step trong `allure-gh-pages.yml` trước `allure generate`

### 2. E2E Test Body trông xấu khi show thầy
**Triệu chứng:** Allure hiện raw CodeceptJS method calls:
```
I am on page "/login"
I wait for element "input[name="email"]", 10
I fill field "input[name="email"]", "collector@test.waste"
I fill field "input[name="password"]", "InvalidPassword123!"
I click "button[type="submit"]"
I wait for text "Email hoặc mật khẩu không đúng", 10
```

**Giải pháp:** Thêm `I.say()` BDD-style steps vào tất cả 3 E2E test files:
```javascript
I.say('[Given] User is on the login page');
I.say('[When] Collector enters valid email but INVALID password');
I.say('[Then] System shows authentication error message');
I.say('[And] URL remains on login page — no redirect');
```

**Files đã sửa:**
- `frontend/e2e/collector_task_test.js` — thêm Given/When/Then + `loginAsCollector()` helper
- `frontend/e2e/citizen_report_test.js` — thêm Given/When/Then + `loginAsCitizen()` helper
- `frontend/e2e/enterprise_assign_test.js` — thêm Given/When/Then + `loginAsEnterprise()` helper
- Đánh số scenario `#1`, `#2`, `#3`, `#4` cho dễ track trong Allure

### 3. SonarCloud workflow fail
**Triệu chứng:** `SonarCloud Analysis #24` fail sau 1m53s

**Nguyên nhân đã fix:**
1. `sonar-project.properties` ở working dir → `dotnet-sonarscanner` không hỗ trợ format này → error
2. `-Dsonar.projectName=KCPM Frontend` có space → bị parse thành 2 args → error

**Fix:**
- Thêm step `mv sonar-project.properties sonar-project.properties.bak` trước scan
- Restore sau khi scan xong
- Quote project name: `"-Dsonar.projectName=KCPM Frontend"`

### 4. Các fix nhỏ khác trong đêm
- `run_id` extraction: thay `grep -o '"id"...' | sed` → `jq -r '.workflow_runs[0].id // empty'`
- `build_site_index.py`: rewrite hoàn toàn (IndentationError ở line 17)
- Thêm E2E suite card vào landing page (trước chỉ có API + xUnit)
- Tạo `README.md` với CI badges + team table + pipeline diagram
- Tạo `sonar-project.properties` cho frontend scanner

---

## SonarCloud Status (tại thời điểm session)
| Project | Status |
|---------|--------|
| `chi-trung_KCPM_backend` | New — chưa analyze (chờ workflow re-run) |
| `chi-trung_KCPM_frontend` | New — chưa analyze (chờ workflow re-run) |
| `KCPM` | Cũ, đã có analysis 6/6/2026 |

**SONAR_TOKEN:** Đã có sẵn trong GitHub Secrets ✅

---

## Commits trong phiên này
```
5b121bd16  feat: fix Allure suites to 3 groups + upgrade SonarCloud + fix build_site_index
f0a622219  feat: README + jq run_id fix + sonar-project.properties  
ff252ca33  docs: add session history 2026-06-12_00h30
5262071a0  fix: resolve 2 SonarCloud errors (sonar-project.properties + project name quoting)
b83a8c387  feat: add BDD-style I.say() steps to all E2E tests for clean Allure report
```

---

## Trạng thái CI sau session
| Workflow | Status |
|----------|--------|
| ✅ Backend Tests | Xanh — 244+ tests passed |
| ✅ Frontend E2E | Xanh — 4m43s |
| 🔄 Allure Pages #233 | Đang/đã chạy với normalize script mới |
| 🔄 CI CD Deploy | Đang chạy |
| ❌→🔄 SonarCloud | Đã fix 2 lỗi — cần re-run |

---

## Lựa chọn phát triển tiếp (đã đề xuất cho user)
| ID | Tính năng | Độ khó | Giá trị |
|----|-----------|--------|---------|
| A | Allure Behaviors tab — thêm @epic/@feature/@story | Trung bình | ⭐⭐⭐ |
| B | Frontend smoke test viết đúng BDD style | Nhỏ | ⭐⭐ |
| C | Allure Environment tab — DB/backend version info | Nhỏ | ⭐⭐⭐ |
| D | Test coverage report — coverlet .NET + SonarCloud | Khó | ⭐⭐⭐⭐ |
| E | Jira integration — auto create/close issues | Khó | ⭐⭐⭐⭐ |
| F | Thêm Postman test cases cho Waste/Collector routes | Trung bình | ⭐⭐⭐ |
